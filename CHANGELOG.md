# Changelog

## 3.1.0

* Add `average` function

## 3.0.1

* TypeScript/TSLint updates

## 3.0.0

* Convert to TypeScript
* Remove `Random` and `Matrix3` (split into separate gamelibs)
* Use `@plato` package scope

## 2.0.0

* Remove optional rounding parameter from `Random.range`
* Add `Random.rangeInt`
* Remove `Random.integer`
* Tweak LCG parameters for better distribution
